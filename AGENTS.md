# Project Codex Instructions

- Use normal, clear prose by default.
- Do not use caveman mode in this project unless the user explicitly requests it for the current task.
- Apply scoped repository guidance from root/nested `AGENTS.md` and `.codex/rules/*.md`; see `docs/DEVELOPMENT.md` for discovery, precedence, and trust boundaries.
